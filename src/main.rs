#![allow(unused)]

mod init_var;
mod dt_structure;
mod strings;
mod stdio;
mod ifelse;
mod loops;
mod ownership;

fn main() {
    // init_var::init_var();
    // dt_structure::tuple();
    // dt_structure::arr();
    // dt_structure::slice()
    // strings::strings();
    // stdio::stdio();
    // ifelse::ifelse()
    // loops::loops();
    ownership::ownershio();
}
