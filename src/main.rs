#![allow(unused)]

mod init_var;
mod dt_structure;
mod strings;
mod stdio;

fn main() {
    // init_var::init_var();
    // dt_structure::tuple();
    // dt_structure::arr();
    // dt_structure::slice()
    // strings::strings();
    stdio::stdio();
}
